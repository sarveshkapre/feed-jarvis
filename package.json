{
  "name": "feed-jarvis",
  "version": "0.0.0",
  "description": "Local-first CLI that turns RSS/Atom or JSON event feeds into persona-branded, tweet-length posts.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "feed-jarvis": "./dist/cli.js"
  },
  "scripts": {
    "dev": "tsx src/cli.ts",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "biome check .",
    "format": "biome format --write .",
    "test": "vitest run",
    "check": "npm run lint && npm run typecheck && npm test && npm run build"
  },
  "dependencies": {
    "fast-xml-parser": "^5.0.0"
  },
  "overrides": {
    "esbuild": "^0.27.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@biomejs/biome": "^2.3.13",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
